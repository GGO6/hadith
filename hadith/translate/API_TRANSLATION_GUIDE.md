# دليل الترجمة باستخدام GPT API

## نظرة عامة

هذا النظام يستخدم **GPT-4o-mini API** مباشرة للترجمة الكاملة، مما يجعلها:
- ⚡ **أسرع بـ 10-20 مرة** من NLLB المحلي
- ⏱️ **15-20 ساعة** بدلاً من 7-10 أيام
- 💰 **تكلفة:** ~$125-150 للترجمة الكاملة

---

## المتطلبات

### 1. OpenAI API Key

احصل على مفتاح API من: https://platform.openai.com/api-keys

### 2. تعيين متغير البيئة

```bash
export OPENAI_API_KEY="sk-your-api-key-here"
```

أو في ملف `.env`:
```
OPENAI_API_KEY=sk-your-api-key-here
```

---

## الاستخدام

### 1. تفعيل البيئة الافتراضية

```bash
cd /Users/osamaamer/Desktop/code/hadith/hadith/translate
source venv/bin/activate
```

### 2. تشغيل الترجمة

#### اختبار (100 حديث فقط):
```bash
python run_api_translation.py --test --languages turkish
```

#### ترجمة كاملة:
```bash
python run_api_translation.py --languages turkish
```

#### ترجمة عدة لغات:
```bash
python run_api_translation.py --languages turkish french spanish
```

#### استخدام API key مباشرة:
```bash
python run_api_translation.py --api-key sk-your-key --languages turkish
```

---

## المميزات

### ✅ السرعة
- **كل حديث:** ~1-2 ثانية
- **100 حديث:** ~2-3 دقائق
- **كتاب البخاري:** ~2-3 ساعات
- **جميع الكتب:** ~15-20 ساعة

### ✅ الجودة
- جودة ممتازة (GPT-4o-mini)
- فهم سياقي للنصوص الدينية
- دقة في المصطلحات الإسلامية

### ✅ الموثوقية
- حفظ تلقائي للتقدم (checkpoints)
- يمكن استئناف الترجمة من أي نقطة
- معالجة أخطاء API تلقائياً

---

## التكلفة

### GPT-4o-mini:
- **Input:** $0.15 لكل 1M tokens
- **Output:** $0.60 لكل 1M tokens
- **متوسط كل حديث:** ~500 tokens
- **50,000 حديث:** ~25M tokens
- **التكلفة الإجمالية:** ~$125-150

### توفير التكلفة:
- استخدم `--test` للاختبار أولاً
- ابدأ بلغة واحدة
- راجع النتائج قبل المتابعة

---

## متابعة التقدم

### استخدام monitor.py:
```bash
python monitor.py
```

### متابعة السجل:
```bash
tail -f translation_api_run.log
```

### التحقق من Checkpoints:
```bash
cat checkpoints/turkish_api_checkpoint.json
```

---

## مقارنة مع NLLB المحلي

| الميزة | NLLB (محلي) | GPT API |
|--------|-------------|---------|
| السرعة | ⭐ بطيء | ⭐⭐⭐⭐⭐ سريع جداً |
| التكلفة | ✅ مجاني | 💰 $125-150 |
| الجودة | ⭐⭐⭐ جيدة | ⭐⭐⭐⭐⭐ ممتازة |
| الخصوصية | ✅✅✅ كاملة | ❌ البيانات ترسل لـ OpenAI |
| الوقت | 7-10 أيام | 15-20 ساعة |

---

## نصائح مهمة

1. **ابدأ بالاختبار:** استخدم `--test` أولاً
2. **راقب التكلفة:** تابع استخدام API في dashboard OpenAI
3. **احفظ التقدم:** Checkpoints تُحفظ تلقائياً
4. **Rate Limits:** النظام يتعامل معها تلقائياً

---

## استكشاف الأخطاء

### خطأ: API key not found
```bash
export OPENAI_API_KEY="sk-your-key"
```

### خطأ: Rate limit exceeded
- النظام ينتظر تلقائياً
- أو استخدم API key آخر

### خطأ: Insufficient credits
- تأكد من وجود رصيد في حساب OpenAI
- تحقق من: https://platform.openai.com/account/billing

---

## الخلاصة

استخدام GPT API هو **الأفضل للسرعة والجودة**:
- ⚡ أسرع بـ 10-20 مرة
- ⭐ جودة ممتازة
- 💰 تكلفة معقولة (~$125)

**ابدأ الآن:**
```bash
export OPENAI_API_KEY="sk-your-key"
python run_api_translation.py --test --languages turkish
```
