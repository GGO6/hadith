# دليل متابعة تقدم الترجمة - خطوة بخطوة

## الطريقة الأولى: استخدام سكريبت المراقبة (الأسهل والأسرع)

### الخطوة 1: افتح Terminal
- اضغط `Cmd + Space` واكتب "Terminal"
- أو افتح Terminal من Applications

### الخطوة 2: انتقل إلى مجلد الترجمة
```bash
cd /Users/osamaamer/Desktop/code/hadith/hadith/translate
```

### الخطوة 3: شغل سكريبت المراقبة
```bash
python monitor.py
```

### ماذا سترى؟
- 📄 حجم ملف السجل
- 📝 آخر 20 سطر من السجل
- 💾 حالة Checkpoints (عدد الأحاديث المترجمة)
- 📦 ملفات الإخراج المحفوظة

### مثال على النتيجة:
```
======================================================================
Translation Progress Monitor
======================================================================

📄 Log File: /Users/osamaamer/Desktop/code/hadith/hadith/translate/translation_run.log
   Size: 622.95 KB

📝 Last 20 lines:
----------------------------------------------------------------------
Translating book: Sahih al-Bukhari (bukhari)
  bukhari:   5%|▌         | 5/97 [25:30<8:15:00, 306.52s/it]

💾 Checkpoints:
----------------------------------------------------------------------
   turkish: 150/10000 (1.5%)
      Last updated: 2026-01-30 15:30:00

📦 Output Files:
----------------------------------------------------------------------
   turkish/all_translations.json: 2.5 MB
```

---

## الطريقة الثانية: متابعة السجل مباشرة (Live Updates)

### الخطوة 1: افتح Terminal

### الخطوة 2: انتقل إلى مجلد الترجمة
```bash
cd /Users/osamaamer/Desktop/code/hadith/hadith/translate
```

### الخطوة 3: شغل الأمر لمتابعة السجل مباشرة
```bash
tail -f translation_run.log
```

### ماذا سيحدث؟
- سترى السجل يتحدث مباشرة
- كل سطر جديد سيظهر فوراً
- سترى التقدم في الوقت الفعلي

### للإيقاف:
- اضغط `Ctrl + C`

### مثال على ما ستراه:
```
Translating book: Sahih al-Bukhari (bukhari)
  bukhari:   1%|          | 1/97 [06:13<9:56:56, 373.09s/it]
Translating chapter 1...
Translating chapter 2...
  bukhari:   2%|▏         | 2/97 [12:25<9:50:00, 372.00s/it]
```

---

## الطريقة الثالثة: التحقق من التقدم مرة واحدة

### الخطوة 1: افتح Terminal

### الخطوة 2: انتقل إلى مجلد الترجمة
```bash
cd /Users/osamaamer/Desktop/code/hadith/hadith/translate
```

### الخطوة 3: شاهد آخر 30 سطر من السجل
```bash
tail -30 translation_run.log
```

### أو للبحث عن معلومات محددة:
```bash
tail -100 translation_run.log | grep -E "Translating|Processing|%|hadith|bukhari|muslim"
```

---

## الطريقة الرابعة: التحقق من Checkpoints (نقاط الحفظ)

### الخطوة 1: افتح Terminal

### الخطوة 2: انتقل إلى مجلد الترجمة
```bash
cd /Users/osamaamer/Desktop/code/hadith/hadith/translate
```

### الخطوة 3: شاهد قائمة Checkpoints
```bash
ls -lh checkpoints/
```

### الخطوة 4: اقرأ محتوى Checkpoint محدد
```bash
cat checkpoints/turkish_checkpoint.json
```

### أو بشكل منسق (إذا كان jq مثبت):
```bash
cat checkpoints/turkish_checkpoint.json | jq
```

### ماذا سترى؟
```json
{
  "language": "turkish",
  "total_hadiths": 10000,
  "translated_count": 150,
  "last_updated": "2026-01-30 15:30:00",
  "books_completed": ["bukhari"],
  "current_book": "muslim",
  "current_chapter": 5
}
```

---

## الطريقة الخامسة: التحقق من ملفات الإخراج

### الخطوة 1: افتح Terminal

### الخطوة 2: انتقل إلى مجلد الترجمة
```bash
cd /Users/osamaamer/Desktop/code/hadith/hadith/translate
```

### الخطوة 3: شاهد قائمة ملفات الإخراج
```bash
ls -lh output/
```

### الخطوة 4: شاهد الترجمات المحفوظة
```bash
# عرض أول 5 ترجمات
cat output/turkish/all_translations.json | jq '.[0:5]'

# أو بدون jq (أول 50 سطر)
head -50 output/turkish/all_translations.json
```

---

## فهم المعلومات التي تراها

### 1. شريط التقدم (Progress Bar)
```
bukhari:   5%|▌         | 5/97 [25:30<8:15:00, 306.52s/it]
```
- `5%`: النسبة المئوية المكتملة
- `5/97`: 5 فصول من أصل 97
- `25:30`: الوقت المنقضي (25 دقيقة و 30 ثانية)
- `8:15:00`: الوقت المتبقي المتوقع
- `306.52s/it`: متوسط الوقت لكل فصل (حوالي 5 دقائق)

### 2. Checkpoint Information
- `translated_count`: عدد الأحاديث المترجمة
- `total_hadiths`: إجمالي الأحاديث
- `last_updated`: آخر تحديث

### 3. Log Messages
- `Translating book: ...`: بدء ترجمة كتاب
- `Translating chapter ...`: بدء ترجمة فصل
- `Saved checkpoint`: تم حفظ نقطة الحفظ
- `Translation Statistics`: إحصائيات الترجمة

---

## نصائح مهمة

### 1. الترجمة بطيئة في البداية
- النموذج يحتاج وقت للتحميل في الذاكرة
- بعد التحميل الأول، ستكون أسرع

### 2. Checkpoints تُحفظ تلقائياً
- كل فترة يتم حفظ التقدم
- يمكنك إيقاف العملية واستئنافها لاحقاً

### 3. الوقت المتوقع
- كل حديث: حوالي 5-10 ثواني
- كل فصل: حوالي 5-10 دقائق
- كتاب كامل: عدة ساعات

### 4. إذا توقفت العملية
- يمكنك استئنافها من آخر checkpoint
- لا حاجة لبدء من جديد

---

## أوامر سريعة للنسخ واللصق

### للتحقق السريع:
```bash
cd /Users/osamaamer/Desktop/code/hadith/hadith/translate && python monitor.py
```

### للمتابعة المباشرة:
```bash
cd /Users/osamaamer/Desktop/code/hadith/hadith/translate && tail -f translation_run.log
```

### للتحقق من التقدم:
```bash
cd /Users/osamaamer/Desktop/code/hadith/hadith/translate && tail -30 translation_run.log | grep -E "Translating|%|hadith"
```

---

## استكشاف الأخطاء

### إذا لم ترَ أي تقدم:
1. تحقق من أن العملية ما زالت تعمل:
   ```bash
   ps aux | grep python | grep run_translation
   ```

2. تحقق من آخر سطر في السجل:
   ```bash
   tail -5 translation_run.log
   ```

3. تحقق من وجود أخطاء:
   ```bash
   grep -i error translation_run.log | tail -10
   ```

---

## الخلاصة

**أسهل طريقة للمتابعة:**
```bash
cd /Users/osamaamer/Desktop/code/hadith/hadith/translate
python monitor.py
```

**للمتابعة المباشرة:**
```bash
cd /Users/osamaamer/Desktop/code/hadith/hadith/translate
tail -f translation_run.log
```
